[workflow]
# Enable per-feature git worktrees at .worktrees/<feature>/ on `centinela start`.
use_worktrees = true

[validate]
commands = [
  "go test ./...",
  "./scripts/check-coverage.sh"
]

[gates]
file_size = true
