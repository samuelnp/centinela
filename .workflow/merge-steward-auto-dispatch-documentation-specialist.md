### Documentation-Specialist Report: merge-steward-auto-dispatch

**Date:** 2026-05-20
**Status:** done

#### KB entry written

- File: `docs/project-docs/kb/merge-steward-auto-dispatch.md`
- Frontmatter: `feature: merge-steward-auto-dispatch`, `audience: end-user`, `status: done`, one-sentence end-user summary.
- All three required sections present: "What it does" (4 plain-language sentences), "When you'd use it" (links back to the [parallel-feature-worktrees](../kb/parallel-feature-worktrees.md) parent guide), and "How it behaves" (one bullet per spec scenario — see scenario count below).
- Optional "Examples" section: shows a clean merge, a conflicted merge with the dispatch directive block, an APPLY `--continue` finalize, and an ESCALATE `--continue` block with proposed diff to stderr. Closes with an explicit note that the parent feature shipped only a printed hint while this feature wires the auto-dispatch through.

#### Spec / scenario count covered

- Spec: `specs/merge-steward-auto-dispatch.feature`
- Scenario count: **13** (clean-merge regression guard, text-conflict dispatch, post-merge-validate-failed dispatch, hook re-emit while pending, hook silent on valid evidence, hook silent on no marker, `--continue` APPLY finalize, `--continue` ESCALATE blocks, `--continue` missing-evidence refusal, `--continue` schema-invalid evidence refusal, `--continue` APPLY but dirty main refusal, `--continue` no-marker clear error, re-running merge rewrites the single marker).
- Every scenario is rendered as a user-visible bullet in the KB's "How it behaves" section.

#### Roadmap / parent-feature closure

- This feature closes the v1.1 follow-up explicitly called out at the bottom of `docs/project-docs/kb/parallel-feature-worktrees.md`:
  > "in this version, `centinela merge` prepares the Merge Steward hint and exits so you can act on it; automatic hand-off to the Steward agent is a planned follow-up."
- Parent surface: `parallel-feature-worktrees` (worktree creation, `centinela merge`, Merge Steward prompt + evidence-contract entry + `RoleMergeSteward` validator branch).
- Gap closed: `centinela merge` now writes `.workflow/<feature>-merge-pending.json` and emits a `CENTINELA DIRECTIVE` instead of stopping at a printed hint; a new `centinela hook merge` UserPromptSubmit re-emits the directive while the marker exists without valid evidence; `centinela merge --continue <feature>` re-validates the steward evidence through the existing `orchestration.ValidateEvidence(... RoleMergeSteward ...)` and gates finalization on APPLY + `handoffTo:complete` + clean main, with ESCALATE/invalid/missing keeping worktree+marker and exiting non-zero.
- `ROADMAP.md` itself only tracks Phase 0 (`docs-migration-managed-docs`); the auto-dispatch closure is recorded against the parent feature's KB note, not in `ROADMAP.md`.

#### Workflow status matrix

| Step      | Role                    | Evidence                                                                       | Status |
|-----------|-------------------------|--------------------------------------------------------------------------------|--------|
| plan      | big-thinker             | `.workflow/merge-steward-auto-dispatch-big-thinker.{md,json}`                  | done   |
| plan      | feature-specialist      | `.workflow/merge-steward-auto-dispatch-feature-specialist.{md,json}`           | done   |
| code      | senior-engineer         | `.workflow/merge-steward-auto-dispatch-senior-engineer.{md,json}`              | done   |
| tests     | qa-senior               | `.workflow/merge-steward-auto-dispatch-qa-senior.{md,json}` + edge-cases.md    | done   |
| validate  | gatekeeper              | `.workflow/merge-steward-auto-dispatch-gatekeeper.md` (SAFE)                   | done   |
| validate  | validation-specialist   | `.workflow/merge-steward-auto-dispatch-validation-specialist.{md,json}` (PASS) | done   |
| docs      | documentation-specialist| this report + JSON                                                              | done   |

Gates: G1 file size diff-aware skip; `go test ./...` green; coverage 95.1% at strict 95% gate; gatekeeper SAFE; production-readiness not enabled (skipped per `centinela.toml`). `centinela validate` exits 0.

#### Outputs (also enumerated in JSON for traceability)

- `docs/project-docs/kb/merge-steward-auto-dispatch.md` (KB markdown source, written by this role)
- `docs/project-docs/kb/merge-steward-auto-dispatch.html` (regenerated by `centinela docs generate`)
- `docs/project-docs/kb/index.html` (regenerated by `centinela docs generate`)
- `docs/project-docs/index.html` (regenerated by `centinela docs generate`)

#### Handoff

- Next role: complete
- No source code changes were required to produce these artifacts.
