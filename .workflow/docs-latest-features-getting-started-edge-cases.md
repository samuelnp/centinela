# Edge Cases: docs-latest-features-getting-started

- README and generated HTML can drift if one gets updated without the other.
- The generated presentation must stay useful even when `ROADMAP.md` is minimal or stale.
- Hook and command documentation must reflect the five-step workflow, not older four-step examples.
