# Edge Cases — refine-ux-specialist-evidence

- `ux-ui-specialist` evidence may reference valid UI files but still omit `mobileFirst: true`.
- UX evidence may include ad-hoc review notes but miss required structured tags like `visual-hierarchy` or `responsive-layout`.
- Non-UX roles must continue to pass without a `mobileFirst` field or UX-specific tag vocabulary.
