- feat: governance-telemetry — foundational, local, append-only JSONL event log of governance friction (`block`, `gate-failure`, `verify-rejection`, `complete-rejected`, `step-advanced`) at `.workflow/telemetry/events.jsonl`, schema `centinela.telemetry/v1`. New `internal/telemetry` leaf package (non-blocking `Record`/constructors + lenient `Read`, mirrors governed-memory's warn-and-continue contract); emitted only from cmd chokepoints (prewrite/validate/complete) so domain producers stay pure. `[telemetry] enabled` (`*bool`, default-on/opt-out). Rework is a derived metric (complete-rejected count before step-advanced); no new workflow state. Unblocks centinela-insights, failure-ledger-plan-advisor, capability-calibration, team-dashboard, adaptive-skill-synthesis. This repo `.gitignore`s its own `.workflow/telemetry/`; the contract for other projects stays git-tracked.
