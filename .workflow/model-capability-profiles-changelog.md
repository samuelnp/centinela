- feat: add model-capability-profiles — a pinned driver model's declared capability (frontier/capable/limited) selects the default enforcement profile via a new lowest-priority precedence tier (below explicit --profile and explicit global enforcement_profile, above the strict default). Configured with `[orchestration] driver_model` (resolved `--model` flag > `CENTINELA_MODEL` env > config), `[orchestration.capabilities]` (model id → class, with built-ins for the Anthropic tier models), and `[orchestration.capability_profiles]` (class → profile override; default frontier→outcome, capable→guided, limited→strict). `centinela status` surfaces the driver model and profile provenance. Zero-config behavior is byte-identical strict — fully back-compat.
