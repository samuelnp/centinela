# Edge Cases: docs-update-migrate-readme

- Keep bootstrap guidance (`init`) and upgrade guidance (`migrate`) clearly separated.
- Ensure setup-only examples include both preview and apply variants.
- Keep hook behavior wording aligned with current migration guidance injection.
