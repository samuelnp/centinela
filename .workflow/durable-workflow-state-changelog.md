- fix: make `.workflow/<feature>.json` survive what actually happens to it — writes go temp-fsync-rename so a killed process can no longer truncate the file, a `schemaVersion` probed from the raw bytes stops an older binary silently dropping fields it does not understand (`Save` fails closed on a version it cannot read), and a compare-and-swap refuses a save whose file changed underneath it. A state file written by a newer Centinela now refuses the write naming `centinela update`, rather than either bricking the repo or waving every write through
