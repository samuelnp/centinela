- feat: docstring gate — `centinela docs lint` walks the Go AST and fails on exported identifiers without doc comments, ratcheted to changed files so legacy debt never blocks while new or modified exports must be documented; `//centinela:nodoc` opt-out is listed on passing runs, and the shared diff resolver now falls back to `origin/<base>` so diff-aware gates resolve a merge base in CI checkouts
