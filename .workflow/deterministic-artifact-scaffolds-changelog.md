- feat: deterministic artifact scaffolds — `centinela evidence init` now pre-fills the plan-snapshot `inputs` (every `docs/features/*.md` + `docs/plans/<feature>.md`) for big-thinker and feature-specialist, so plan agents no longer hand-loop the snapshot. Adds a canonical `<FILL: …>` marker, per-role companion-markdown skeletons, and `artifact new` body upgrades (gatekeeper "Analyzed Specs" is pre-filled from `specs/*.feature`). `RequiredPlanInputs` is now exported and shared by the pre-fill and the plan-snapshot validator (which now also requires the plan path, matching the documented contract). No schema change; `<FILL:` never enters evidence JSON.
