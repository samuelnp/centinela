- fix: truthful validators — acceptance runs with skipped, pending, undefined or zero scenarios now fail validate (tier-attributed, union-parsed across cucumber/godog/go-test, configurable via validate.acceptance_skip_policy); evidence validate loads real ui_paths instead of nil; quality-score shape errors name the expected schema; G1 reports Skip on an empty diff scope and G11 reports single-locale parity as trivial rather than a confident pass
