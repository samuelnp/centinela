---
id: 28db0c5dc1738aef
feature: workflow-archetypes
step: validate
type: verdict
title: ### Gatekeeper Report: workflow-archetypes
tags: gatekeeper, verdict
sourceArtifact: .workflow/workflow-archetypes-gatekeeper.md
createdAt: 2026-06-12T12:32:40Z
---

### Gatekeeper Report: workflow-archetypes
**Date:** 2026-06-12
**Status:** SAFE

#### Analyzed Specs
- `specs/bootstrap-phase-zero-workflow.feature` — materially relevant: pins greenfield bootstrap order (`plan, code, validate`) and existing-project default start. The archetype resolver wraps `workflowOrderForFeature` unchanged, so all three bootstrap scenarios still resolve via the original path when no archetype is chosen.
- `specs/enforcement-profiles.feature` — materially relevant: archetype (sequence) is orthogonal to profile (strictness). No shared code; spike+strict combine cleanly. The new `--archetype` flag mirrors `--profile` and both normalize independently.
- `specs/workflow-archetypes.feature` (this feature) — 11 scenarios, all with acceptance coverage (spec-traceability 11/11).
- `specs/opencode-setup-question-parity.feature` — references "architecture archetype" (Hexagonal/Rails/ECS), an unrelated concept; no overlap with workflow archetypes. No conflict.

#### Findings
No conflicts detected. Verified:

1. **Default (canonical) preservation — PASS.** `resolveArchetypeOrder` (start_guard.go:18) returns `archetypeOrderByName` only when a flag or roadmap `Feature.Archetype` is present; otherwise it falls through to `workflowOrderForFeature(feature)` (the untouched bootstrap/canonical selector) and labels it `ArchetypeCanonical`. `DefaultStepOrder`/`BootstrapStepOrder` are unchanged; `New()` still constructs with `DefaultStepOrder`. Existing assertions (`order_profile_test.go`, `order_test.go`, `bootstrap_phase_zero_workflow_integration_test.go`) all pass.

2. **start.go:84 fix — PASS, no regression.** `ui.RenderStep("Current step", order[0])` replaces the hardcoded `"plan"`. For a canonical start `order[0] == "plan"`, so the printed output is identical to before; no test pinned the `"Current step"` literal (only generic `RenderStep` smoke tests exist, unaffected).

3. **Spike safety property (CRITICAL) — PASS.** `git grep spike` over `cmd/centinela/complete.go internal/verify internal/gates` is empty (exit 1). `git diff --stat main...HEAD -- cmd/centinela/complete.go internal/verify internal/gates` is EMPTY. The ship gate at complete.go:51 remains `if current == "validate"` — step-keyed, not archetype-keyed. Spike's order is `[plan, code]` (no validate), so the gate simply never fires; this is ungated-by-absence, not a bypass branch. Pinned by `archetype_safety_test.go`.

4. **Workflow.Archetype state additive — PASS.** Field is `json:"archetype,omitempty"`, absent for pre-existing workflows; `DisplayArchetype` falls back to canonical for unset. No JSON round-trip/snapshot test breaks (full suite green).

5. **roadmap Feature.Archetype + ValidateDependencies — PASS.** `Feature.Archetype` is `omitempty`; `ValidateDependencies` now calls `workflow.ValidateArchetype(f.Archetype)`, which accepts empty. The repo's own `roadmap.json` (no archetype fields) still validates: `/tmp/cent-gk3 roadmap validate` → valid. No new import edge (roadmap already imports workflow); no cycle.

6. **NewWithOrder / start signature — PASS.** `resolveArchetypeOrder` wraps `workflowOrderForFeature` without altering its signature; `NewWithOrder` is unchanged and `wf.Archetype` is set post-construction as metadata. No test signatures break.

Non-default archetype behaviors (hotfix starting at `code`, spike omitting `validate`) are opt-in and only reachable via explicit flag or roadmap pinning — they describe new behavior, not changes to existing specs.

#### Recommendation
- **SAFE** — additive, opt-in start-time preset; the ship gate and all verify/gate code are byte-for-byte unchanged (empty diff), default/bootstrap paths preserved, full suite (1342) green, `validate` (11/11 spec-traceability) and `roadmap validate` both pass.
