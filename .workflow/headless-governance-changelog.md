- feat: headless-governance — non-interactive parity + machine-readable verdict packet. Adds `config.IsHeadless` (precedence: `CENTINELA_HEADLESS` env > `[headless] enabled` > `detect_ci && CI`), which short-circuits the step-review and plan-advisor hooks to silence (headless wins over explicit `step_confirmation_mode`/`plan_advisor_mode`); CI auto-detect is opt-in. Adds the `internal/verdict` package + `centinela verdict <feature>` command emitting a deterministic `centinela.verdict/v1` JSON packet (run info + gates + verify checks + evidence index + summary) to stdout, exit 0 pass / 1 fail. Default-off → byte-identical back-compat; gates/verify unchanged.
