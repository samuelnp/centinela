# claim-verification — Documentation Specialist Report

**Date:** 2026-05-29
**Status:** done
**Handoff to:** complete

## Summary

This report covers the documentation step for the `claim-verification` feature. All required docs artifacts were produced and validated. No source code was modified.

## Inputs Consulted

- `docs/architecture/documentation-generator-prompt.md` — KB contract and authoring rules
- `docs/architecture/evidence-contract.md` — JSON schema, per-role rules, `coverage` field spec
- `docs/features/claim-verification.md` — Feature brief, user stories, acceptance criteria, edge cases
- `docs/plans/claim-verification.md` — Layered design, slice plan, config knobs, risk table
- `specs/claim-verification.feature` — 17 Gherkin scenarios covering all four claim checks
- `.workflow/claim-verification-senior-engineer.md` — Implementation inventory and G2 flag for `internal/verify`
- `.workflow/claim-verification-qa-senior.md` — Test inventory, coverage measurements (95.2%), scaffold mirror sync note
- `.workflow/claim-verification-gatekeeper.md` — WARNING: G2 governance gap on `internal/verify`; all blocking gates PASS
- `.workflow/claim-verification-validation-specialist.md` — Confirmed WARNING status, mandated G2 update in docs step
- `PROJECT.md` — G2 rule (prior wording), architecture overview
- `README.md` — Existing command and gate structure

## Outputs Produced

### docs/project-docs/kb/claim-verification.md

New KB entry following the required frontmatter + three mandatory sections contract. Written in plain language for Centinela end-users. Covers all 17 spec scenarios as user-visible behaviors in "How it behaves", includes an "Examples" section with sample CLI output and the `[verify]` TOML snippet.

### PROJECT.md — G2 rule update

Extended the G2 sentence to explicitly name `internal/verify` and its allowed imports, closing the governance gap flagged by the gatekeeper and required by the validation-specialist. No other content changed.

### README.md — three additions

1. **Latest Features** — added a "Claim verification" bullet describing `centinela verify` and its role as a HARD block in `centinela complete`.
2. **Workflow commands** — added `centinela verify <feature>` to the command table with a one-line description.
3. **Gate Checks** — added a "Claim verification at the validate step" subsection above the built-in gates table, explaining the HARD block, the warn-only edge-case check, and a pointer to the `[verify]` config block.
4. **centinela.toml Reference** — added the `[verify]` TOML block documenting `verify_timeout` (default 60 seconds) and `coverage_tolerance` (default 0.001 = 0.1%).

### docs/project-docs/index.html + kb/claim-verification.html + kb/index.html

Regenerated by `centinela docs generate --out docs/project-docs/index.html`. All three HTML files updated.

## Edge Cases Addressed

- **Coverage tolerance boundary:** the KB Examples section shows both a PASS (within 0.1%) and a FAIL (above tolerance) scenario, making the boundary concrete for users.
- **CONFIG ERROR vs FAIL:** the KB explicitly distinguishes a missing/misconfigured `validate.commands` (CONFIG ERROR) from a fabricated claim (FAIL), matching the spec distinction.
- **Edge-case mapping is warn-only:** both the KB and the README Gate Checks section make clear that unmatched edge cases produce a WARN that does not hard-block `centinela complete`.

## Validation

`centinela docs validate` — passed before writing any output.
`centinela docs generate` — exited 0; `kb/claim-verification.html`, `kb/index.html`, and `docs/project-docs/index.html` all regenerated.
