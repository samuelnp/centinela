- feat: pre-commit + PR gates — `centinela precommit` runs the mechanical gates against your **staged** changes and blocks the commit on a failure (fast: skips the cross-compile gate by default); `centinela precommit install` wires an idempotent, non-clobbering `.git/hooks/pre-commit`. `centinela pr-gate` runs the gates against a PR's changed files and emits a deterministic Markdown verdict; CI posts/updates it as a PR comment. Custom gates and the audit ratchet participate in both. Config: `[precommit]` (`skip_build`) and `[pr_gate]` (`fail_on_warning`).
