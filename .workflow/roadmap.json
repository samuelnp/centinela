{
  "phases": [
    {
      "name": "Phase 0: Bootstrap",
      "features": [
        {
          "name": "docs-migration-managed-docs",
          "description": "managed, incrementally upgradable scaffolded docs via `centinela migrate`."
        }
      ]
    },
    {
      "name": "Phase 1: Harness Capabilities",
      "features": [
        {
          "name": "governed-project-memory",
          "description": "a structured, git-tracked memory ledger that harvests Centinela's own artifacts (edge-case lessons, gate verdicts, decisions) at step boundaries and recalls the relevant slice into later features via the existing context-injection hook. Governed (reviewable, deterministic, no semantic-store dependency), not fuzzy recall."
        }
      ]
    },
    {
      "name": "Phase 2: Configurable Model Routing",
      "features": [
        {
          "name": "configurable-model-routing",
          "description": "runner-keyed tier→model remapping plus per-role concrete-model overrides (`claude` | `opencode` | `codex`), so operators route reasoning/coding work to the models they actually want while every unconfigured role keeps the built-in default. The foundation the capability-adaptive phase builds on."
        }
      ]
    },
    {
      "name": "Phase 3: Close the Mechanical-Verification Gap",
      "features": [
        {
          "name": "g2-import-graph-gate",
          "description": "Mechanically enforce per-archetype layer-dependency rules by parsing the import graph (Go `go/packages`, TS madge/ts-morph, Python AST). Turns the prose G2 rule in `PROJECT.md` into a checkable allow/deny matrix.",
          "fixes": "agent silently introduces a forbidden cross-layer import that no gate catches."
        },
        {
          "name": "security-gate",
          "description": "Mechanical secret-scanning + dependency-vuln audit (`gitleaks` / `osv-scanner` / `govulncheck`, configurable) wired into validate.",
          "fixes": "agent commits a secret or pulls a vulnerable dependency; subagent review misses it."
        },
        {
          "name": "spec-traceability-gate",
          "description": "Verify every Gherkin scenario in `specs/*.feature` maps to an executed step definition in `tests/acceptance/`.",
          "fixes": "spec and acceptance tests drift apart; scenarios silently go unimplemented."
        },
        {
          "name": "g2-multi-language-import-graph"
        }
      ]
    },
    {
      "name": "Phase 4: Loop Velocity",
      "note": "**Pulled forward (2026-06-11).** The per-feature loop charges full ceremony —\nseven sequential specialist roles, five manual confirmations, full docs\nregeneration — regardless of feature size or surface. The 5-step is\nfeature-shaped and frontier-shaped; real work also includes fixes, refactors,\nand spikes. These features cut the round-trips while keeping every gate and\nclaim verification intact, and they pay back on every remaining roadmap item —\nso they ship before the long tail. Verification stays constant; only\n*requested* process shrinks.\n\n**Dogfood note (2026-06-10).** The `code-quality-hardening` fix (PR #23) was\nbuilt through the full five-step workflow with seven specialist subagents and\nfive manual confirmations — ~370K subagent tokens — for a ~250-line semantic\nchange across ten source files. The discipline paid off (subagents corrected\ntwo reviewer claims, caught two missing spec scenarios, and independently\nre-verified the highest-risk change), but the ceremony-to-change ratio on an\ninternal fix is exactly the cost `enforcement-profiles` (an `outcome` profile)\nand `right-size-docs-step` exist to cut: same gates and claim verification, a\nfraction of the round-trips. First-party evidence that this phase earns its\nplace.",
      "features": [
        {
          "name": "enforcement-profiles",
          "description": "Named governance strictness presets — `strict` (the back-compat default: full step-gating, per-step confirmation, mandatory subagent evidence — exactly today's behavior), `guided` (a lighter opt-in: step-gating on, but no mandatory subagent ceremony and review only after planning), `outcome` (work in any order, prompts suppressed; `complete`/merge still requires all gates + claim verification green) — selectable in `centinela.toml` per project and overridable per feature with `centinela start --profile`. Decouples *how much process is enforced* from *whether outcomes are verified*; the latter is constant across all profiles.",
          "fixes": "one-size enforcement either burdens a strong model with ceremony or under-scaffolds a weak one — both end in governance being switched off."
        },
        {
          "name": "workflow-archetypes",
          "description": "First-class lightweight tracks beside the 5-step: `hotfix` (reproduce → fix → test → ship), `refactor` (characterize → change → prove-equivalent), `spike` (timeboxed, no ship gate).",
          "fixes": "forcing a diagnosis/bugfix through a plan→docs pipeline it doesn't fit."
        },
        {
          "name": "right-size-docs-step",
          "description": "Make the `docs` step surface-aware, mirroring how the `code` step requires `ux-ui-specialist` only for `surface: user-facing` features. A user-facing feature still writes the plain-language KB guide (the one genuinely valuable, reader- and memory-useful doc artifact); an internal refactor / bugfix / chore instead emits a one-line changelog entry (via `delivery-artifact-generation`, stubbed as a plain entry until that feature ships) and skips the KB guide, the per-feature HTML portal regeneration, and the documentation-specialist evidence ceremony. The 108 KB `index.html` portal moves to merge/release-time regeneration rather than per-feature.",
          "fixes": "mandatory full docs ceremony — KB guide, HTML regen, evidence — runs on every feature including internal ones with no end-user story, burning tokens for zero reader value."
        }
      ]
    },
    {
      "name": "Phase 5: Operability & DX",
      "note": "Reduce the friction of running and recovering Centinela itself — keep its own\nartifacts honest and self-healing. Cheap, dependency-free wins that smooth\nevery later phase, so they come early.",
      "features": [
        {
          "name": "roadmap-doc-sync",
          "description": "Treat `.workflow/roadmap.json` as the source of truth and generate the human-readable `ROADMAP.md` from it, with a drift check that fails if the two disagree. *(Dogfood note: this roadmap has now drifted twice while being maintained by hand — exactly the toil this removes.)*",
          "fixes": "the machine roadmap and the human roadmap drift apart because they're maintained by hand."
        },
        {
          "name": "centinela-doctor",
          "description": "`centinela doctor`: one command that diagnoses (and, where safe, repairs) broken hook wiring, stale or orphaned `.workflow` state, abandoned worktrees, config drift, and roadmap drift — extending the existing `evidence repair` into a holistic health check.",
          "fixes": "a broken or drifted install fails opaquely with no guided path back to a healthy state — the graceful-recovery gap."
        },
        {
          "name": "deferred-findings-roadmap-capture",
          "description": "When plan-step agents (big-thinker, feature-specialist) detect something outside the current feature's scope, or code/tests-step agents (senior-engineer, qa-senior) surface findings they won't fix immediately, route that information into the roadmap instead of losing it: a `centinela roadmap` capture path that appends the finding to the roadmap together with the analysis/quality entries `roadmap validate` demands, plus contract updates in the four role prompts (and their scaffold mirrors) making the capture mandatory whenever such a finding exists. Exact capture surface (dedicated backlog phase vs deferred-findings ledger promoted at triage) is decided at plan.",
          "fixes": "out-of-scope discoveries and deferred fixes live only in per-feature prose artifacts (Out-of-Scope, Residual Risks, Outstanding TODOs) and evaporate — they never reach the roadmap, the single planning source of truth."
        },
        {
          "name": "cli-self-update",
          "dependsOn": [],
          "description": "Add `centinela update`: a self-update command plus a passive startup version notice. The command resolves the latest GitHub Release, downloads the prebuilt asset matching the host OS/arch (`centinela-v<tag>-<goos>-<goarch>[.exe]`), verifies it against the release `SHA256SUMS`, and atomically replaces the running binary (write a temp file in the SAME directory as the target, fsync, copy the existing mode bits, then `os.Rename`). A `--check` flag reports availability without installing. The startup notice is a throttled, non-blocking check wired into the existing SessionStart hook, comparing the running version to the latest release tag. The version-check cache is a single JSON file at an XDG-rooted path: `${XDG_CACHE_HOME:-~/.cache}/centinela/update-check.json` holding the last-seen latest tag and a unix timestamp; a check within the TTL (default 24h) reads the cache and performs NO network call. Lives in a new leaf package `internal/selfupdate` (net/http + crypto/sha256 + os + encoding/json only); `cmd/centinela` wires the `update` command and the notice into the session hook. All HTTP (GitHub releases API, asset download, SHA256SUMS) is injected behind an interface so tests drive an `httptest.Server`. Acceptance: (1) on an outdated binary, `centinela update` downloads the matching asset, the SHA256 matches `SHA256SUMS`, the binary is replaced atomically, and it prints old->new versions; on the latest it is a no-op printing `already up to date`. (2) `centinela update --check` performs the version check (honoring the TTL cache), prints the availability verdict, exits non-zero when a newer version exists and zero when current, and makes ZERO writes to the binary or any temp file. (3) A `SHA256SUMS` mismatch aborts WITHOUT touching the installed binary (fail-safe) and removes the temp file. (4) An unsupported platform / missing matching asset returns a clear typed error with no partial write. (5) When the target binary's directory is not writable (permission denied), `centinela update` returns a clear typed error, leaves the installed binary untouched, and cleans up any temp file created before the failure. (6) The startup notice is cache-throttled (a second start within the TTL performs no network call — asserted against the httptest call count), fails silent when offline/rate-limited (no error, no block), and appears only when running < latest. (7) GitHub API + asset download + SHA256SUMS + the cache file are exercised against an `httptest.Server` and a temp HOME/XDG dir so tests are deterministic and offline.",
          "fixes": "users running an installed release binary have no in-tool upgrade path — they must manually rebuild or re-download, so they silently keep running stale governance."
        }
      ]
    },
    {
      "name": "Phase 6: Capability-Adaptive Governance",
      "note": "One-size enforcement fails at both ends of the model spectrum: it taxes a\nfrontier model with ceremony it no longer needs, and it under-scaffolds a\nsmall local model that needs *more* rails, not fewer. Both failure modes end\nthe same way — the user disables governance. Building on the\n`enforcement-profiles` presets shipped in Phase 4, this phase makes the\namount of process a function of model capability, while verification stays\nconstant for everyone.",
      "features": [
        {
          "name": "model-capability-profiles",
          "dependsOn": [
            "enforcement-profiles",
            "configurable-model-routing"
          ],
          "description": "A registry mapping each configured model — cloud or local — to a declared capability profile (instruction-following reliability, tool-use reliability, context budget) that selects its default enforcement profile and routing tier. Declarable in `centinela.toml` so any Ollama / llama.cpp / OpenAI-compatible local model is as first-class as a frontier model.",
          "fixes": "model assumptions are hardcoded for frontier cloud models; a local model has no place to declare what it can and cannot reliably do."
        },
        {
          "name": "deterministic-artifact-scaffolds",
          "dependsOn": [
            "enforcement-profiles"
          ],
          "description": "Pre-generated artifact skeletons with explicit fill-in slots (plan, spec, edge-case analysis) plus mechanical generation wherever content is derivable from existing state — extending the proven docs CLI-fallback pattern. Under the `strict` profile, weak models fill constrained templates instead of inventing structure.",
          "fixes": "a low-capability model fails artifact contracts on shape rather than substance, burning retries; the dumbest models need rails that are physical, not prose instructions."
        },
        {
          "name": "headless-governance",
          "description": "Full non-interactive parity: every confirmation prompt gets a config/flag equivalent, and every run can emit a machine-readable end-of-run verdict packet (gate results, verify results, evidence index) as the reviewable output. The foundation for CI, Capataz fleets, and reviewing agent work by evidence instead of by transcript.",
          "fixes": "prompts assume a human in a chat session; unattended runs stall on questions or silently bypass them."
        }
      ]
    },
    {
      "name": "Phase 7: Instrument the Loop",
      "note": "You can't \"fix failures permanently\" if you can't see them. Make Centinela\nobserve itself — including how each *model* actually performs under\ngovernance.",
      "features": [
        {
          "name": "governance-telemetry",
          "description": "Local, git-tracked append-only event log of every block, gate failure, verify rejection, and rework cycle (no external service, consistent with the governed-memory design)."
        },
        {
          "name": "centinela-insights",
          "dependsOn": [
            "governance-telemetry"
          ],
          "description": "Reads telemetry and reports most-triggered blocks, most-failed gates, features with the most rework, and mean steps-to-green.",
          "fixes": "roadmap prioritization by anecdote instead of evidence."
        },
        {
          "name": "failure-ledger-plan-advisor",
          "dependsOn": [
            "governance-telemetry",
            "governed-project-memory"
          ],
          "description": "Feed recurring gate failures from the ledger into the plan advisor so the next feature is pre-warned about the failure modes that actually bite this repo."
        },
        {
          "name": "capability-calibration",
          "dependsOn": [
            "governance-telemetry",
            "model-capability-profiles"
          ],
          "description": "Read per-model telemetry (block rate, gate failures, verify rejections, rework cycles) and report whether each model is over- or under-governed, recommending an enforcement-profile change backed by evidence. Answers \"how much scaffolding does *this* model need on *this* repo\" with measurement instead of vibes.",
          "fixes": "enforcement-profile assignment by intuition; a model that quietly needs tighter (or looser) governance is never recalibrated."
        }
      ]
    },
    {
      "name": "Phase 8: Continuous Governance",
      "note": "Governance currently evaporates at merge. Extend it beyond build-time —\nand open the gate engine so teams bring their own rules.",
      "features": [
        {
          "name": "custom-gate-sdk",
          "dependsOn": [
            "g2-import-graph-gate"
          ],
          "description": "Let teams define their own mechanical gates (project-specific rules) via config/plugin and run them inside `centinela validate` — without forking Centinela. Generalizes the built-in gate interface so the built-in gates become reference implementations, not the ceiling. This is the pivot from \"opinionated workflow tool\" to \"policy engine\": Centinela's own rules become the default profile, not the architecture.",
          "fixes": "teams with bespoke rules have no enforced path short of forking, so those rules stay \"requested, not enforced.\""
        },
        {
          "name": "audit-baseline-ratchet",
          "dependsOn": [
            "g2-import-graph-gate"
          ],
          "description": "Whole-repo gate scan that records a baseline and *ratchets*: never lets new violations in, lets teams pay down old ones over time. Enables adoption on legacy codebases without a big-bang cleanup.",
          "fixes": "the \"we'll never reach zero so we disable the gate\" failure."
        },
        {
          "name": "precommit-and-pr-gate",
          "dependsOn": [
            "audit-baseline-ratchet"
          ],
          "description": "Run the mechanical gates as a fast pre-commit hook and post gate verdicts as PR review comments.",
          "fixes": "violations discovered only at the end of the loop, maximizing rework cost."
        }
      ]
    },
    {
      "name": "Phase 9: Brownfield Onboarding",
      "note": "Centinela excels on greenfield projects, where it interviews the user and\ngenerates a roadmap from a blank slate. On an existing codebase the truth\nalready lives in the code and must be *reverse-engineered*, not interviewed.\nThis phase makes Centinela adoptable on a mature repo without a big-bang rewrite.",
      "features": [
        {
          "name": "deep-codebase-analysis",
          "description": "`centinela analyze`: scan the existing repo and produce a machine-readable inventory — language(s), framework, build/test setup, i18n locales, module/package layout, and the current import/dependency graph. The foundation every other brownfield feature reads from."
        },
        {
          "name": "archetype-inference-project-synthesis",
          "dependsOn": [
            "deep-codebase-analysis"
          ],
          "description": "From the analysis, infer the best-fit architecture archetype and draft a complete `PROJECT.md` (layer mapping, folder structure, naming conventions, gatekeeper paths) reflecting the code *as it is*, for the user to confirm or correct — instead of interviewing from scratch.",
          "fixes": "forcing a brownfield user to hand-author PROJECT.md and guess an archetype that doesn't match reality."
        },
        {
          "name": "spec-reconstruction",
          "dependsOn": [
            "deep-codebase-analysis"
          ],
          "description": "Have the LLM read existing modules, endpoints, and flows and generate `specs/*.feature` Gherkin scenarios plus `docs/features/*.md` briefs that document the behavior the system *already* exhibits, as confirmable, editable acceptance criteria.",
          "fixes": "an existing system with zero specs, so Centinela's spec-first gates have nothing to anchor to."
        },
        {
          "name": "brownfield-roadmap-generation",
          "dependsOn": [
            "archetype-inference-project-synthesis",
            "spec-reconstruction"
          ],
          "description": "Generate a roadmap that distinguishes already-built capability (recorded as baseline / done) from the net-new work and gaps (TODOs, incomplete areas, user-stated goals), so existing functionality isn't re-planned and the team can immediately `centinela start` the next real gap.",
          "fixes": "a greenfield roadmap generator that assumes everything is still ahead of you."
        },
        {
          "name": "adoption-baseline",
          "dependsOn": [
            "deep-codebase-analysis",
            "audit-baseline-ratchet"
          ],
          "description": "On brownfield init, record current gate violations (pre-existing G1 file-size, G2 layer, etc.) as an accepted baseline so `validate` isn't drowned by thousands of legacy findings; only new work is gated strictly while legacy debt is ratcheted down over time.",
          "fixes": "a mature repo where day-one `validate` reports thousands of pre-existing violations, making the gates unusable on adoption."
        }
      ]
    },
    {
      "name": "Phase 10: Delivery",
      "note": "Completed work should ship with its evidence — and the humans reviewing\nagent output at scale review evidence, not transcripts.",
      "features": [
        {
          "name": "completion-delivery-prompt",
          "description": "When a feature reaches completion (final step done), Centinela asks how to deliver it and acts on the choice: **(a)** commit, push, and open a PR via `gh` when a git `origin` remote is configured, or **(b)** merge the branch into `main` locally using the merge-steward (`centinela merge <feature>`, with `--continue` recovery on conflicts). Detects remote presence to offer only valid options; never pushes or merges without explicit confirmation.",
          "fixes": "completed work stalls in its worktree because the agent doesn't know the team's delivery convention, or merges/pushes without asking."
        },
        {
          "name": "delivery-artifact-generation",
          "dependsOn": [
            "completion-delivery-prompt"
          ],
          "description": "Compose the PR description and a `CHANGELOG` / release-notes entry automatically from evidence Centinela already holds — the feature brief, plan, gatekeeper report, and verify results — so delivery output is consistent and traceable instead of hand-written each time.",
          "fixes": "rich step evidence exists but the PR body and changelog are still written from scratch (or skipped)."
        },
        {
          "name": "team-dashboard",
          "dependsOn": [
            "governance-telemetry"
          ],
          "description": "Aggregate view across worktrees and contributors: who owns what feature/step, aggregate gate health, roadmap burn-down.",
          "fixes": "multi-feature, multi-contributor state is invisible without polling each worktree by hand."
        },
        {
          "name": "cost-governance",
          "description": "Per-feature/per-step token and model-tier budgets surfaced from the host harness, with a soft gate. For local models the budget unit is wall-clock/compute rather than spend, but the runaway-step problem is identical.",
          "fixes": "a runaway agent burning budget on one step with no visibility."
        }
      ]
    },
    {
      "name": "Phase 11: Ecosystem",
      "features": [
        {
          "name": "host-harness-adapters",
          "description": "Extend the hook/parity pattern (today Claude + OpenCode) to Cursor, Aider, Windsurf, and Copilot Workspace. Best done after a shared integration core, so adding a third target isn't a maintenance tax."
        },
        {
          "name": "codex-support",
          "dependsOn": [
            "host-harness-adapters"
          ],
          "description": "First-class support for OpenAI **Codex** as a host harness alongside Claude Code and OpenCode: wire Codex's config/integration surface to Centinela's prewrite enforcement, postwrite status tags, and prompt-context injection, generate the Codex-native rules file (e.g. `AGENTS.md`), and extend the parity tests so the three harnesses stay in lockstep through `init` and `migrate`. Codex has a large and growing user base, so dual-support with Claude Code materially widens who can adopt Centinela.",
          "fixes": "Codex users locked out of Centinela's governance because enforcement is wired only for Claude Code / OpenCode."
        },
        {
          "name": "local-harness-support",
          "dependsOn": [
            "host-harness-adapters",
            "model-capability-profiles"
          ],
          "description": "First-class local-model targets: OpenCode backed by Ollama, plus generic OpenAI-compatible endpoints (llama.cpp, vLLM, LM Studio). Acceptance bar: a small local model completes a governed feature end-to-end using the `strict` profile and deterministic scaffolds, with all gates and claim verification passing.",
          "fixes": "local-model users — the fastest-growing segment that needs governance the most — can't adopt Centinela because integration and enforcement assume frontier cloud harnesses."
        },
        {
          "name": "cross-project-memory",
          "dependsOn": [
            "governed-project-memory"
          ],
          "description": "Promote governed-memory lessons from per-repo to an org-shared ledger, so a failure fixed in one repo warns every repo."
        },
        {
          "name": "mcp-governance-server",
          "dependsOn": [
            "headless-governance"
          ],
          "description": "Expose Centinela's rules, gate engine, and claim-verification as a versioned MCP server (v1 tools: read_rules, run_gates, verify_claims, workflow_state) reusing the headless-governance verdict packet as the wire payload. The MCP server is advisory-by-protocol: it returns a structured verdict (allow/warn/block) — it cannot itself stop a write — so enforcement stays harness-side via a thin shim that maps a block verdict onto the harness's existing pre-write deny (the same contract the native hook uses). The tool surface and verdict schema are explicitly versioned so harnesses pin a compatibility level. Acceptance: a harness with zero Centinela-specific code obtains a verdict purely through MCP tool calls; the shim test asserts a block verdict aborts the write while allow proceeds; a parity test asserts the MCP verdict and the native-hook verdict are identical for the same diff and workflow state.",
          "fixes": "every new host harness needs a bespoke hook/parity adapter, so governance can only be consumed by harnesses Centinela has explicitly integrated."
        },
        {
          "name": "agents-md-canonical-surface",
          "dependsOn": [],
          "description": "Make AGENTS.md the canonical rules surface via a managed region fenced by `<!-- centinela:managed:start -->` / `<!-- centinela:managed:end -->`: init/migrate emit Centinela's hard rules and active profile into that region, and Centinela re-ingests a constrained, documented directive schema (rule toggles, profile, locales, layer dependency overrides) authored outside the managed block. Free-form prose outside the managed block and outside the known schema is preserved verbatim and ignored by the rule engine — never silently honored — and any directive that conflicts with a hard rule surfaces as a lint WARNING rather than an override. Acceptance: init produces a valid AGENTS.md with the fenced managed region; round-trip (emit -> hand-edit outside the region -> re-emit) preserves user content byte-for-byte; a known-schema directive is ingested into the rule set; a conflicting directive produces a lint warning; an unrecognized free-form line is preserved and ignored.",
          "fixes": "Centinela's rules live in a Centinela-specific surface, so the 60K+ repos and AGENTS.md-reading harnesses cannot read or contribute to Centinela governance."
        },
        {
          "name": "sdd-spec-kit-interop",
          "dependsOn": [
            "spec-traceability-gate"
          ],
          "description": "Position Centinela as the enforcement layer for Spec-Driven Development with a pinned mapping: GitHub Spec Kit `spec.md` requirements -> Gherkin Scenarios in specs/*.feature, `plan.md` -> docs/plans, `tasks.md` -> step checklist; AWS Kiro requirements map the same way. Each imported requirement receives a stable traceability ID linked to its Scenario; 'links intact' means spec-traceability-gate tracks that ID and FAILS when no code/test covers it. Export regenerates a Spec-Kit-compatible `spec.md` from Centinela's Gherkin + plan artifacts, round-tripping lossily-mapped external fields (e.g. Kiro design rationale) through a preserved sidecar block. External formats are pinned to committed fixtures (Spec Kit v0.8.x and a Kiro schema snapshot under testdata). Acceptance: a fixture spec.md imports with traceability IDs attached; the gate fails when a tracked requirement loses coverage; export round-trips the fixture without dropping sidecar fields.",
          "fixes": "teams adopting Spec-Driven Development (Spec Kit, Kiro) generate specs that nothing enforces, so spec and shipped code silently diverge."
        },
        {
          "name": "agent-action-provenance",
          "dependsOn": [
            "governance-telemetry"
          ],
          "description": "Produce a hash-chained, signed provenance trail of every gate verdict, claim-verification result, and step transition, exported as an in-toto Statement carrying a SLSA v1 provenance predicate. Signing uses a repo-scoped key created by `centinela attest init` (private key in durable .workflow state or a git-config-referenced path; public key committed for verification); org deployments may swap in a KMS/sigstore backend behind the same signer interface (interface only in v1). Each record names the model/harness that made the claim and the gate that cleared it. Acceptance: each completed step appends a hash-chained record; `centinela attest <feature>` emits a verifiable in-toto/SLSA bundle; verification passes on an untampered bundle and fails when any prior record is altered; the bundle attributes each claim to its model/harness.",
          "fixes": "gate verdicts and claim-verification results aren't tamper-evident or exportable, so an enterprise can't audit or attest to what an agent was permitted to do."
        },
        {
          "name": "agent-threat-security-gate",
          "dependsOn": [
            "security-gate"
          ],
          "description": "Extend the security gate with per-class agent-native threat detection: (a) secret/credential exfiltration — deterministic regex+entropy signature scan over added diff lines, plus flagging new outbound-network calls introduced adjacent to a secret read; (b) prompt injection — a heuristic signature set over agent-fetched/MCP-sourced content, explicitly best-effort (WARNING, not a completeness claim); (c) unexpected capability — a diff introducing a new network/exec/credential API absent from the pre-change tree. Acceptance: runs against a committed corpus (testdata/agent-threats/ with planted-positive and clean cases); a planted exfiltration diff BLOCKs under the strict profile; an injected-instruction fixture WARNs; zero false positives on the existing security-gate clean corpus (the numeric bar = 0 FP on that fixed corpus).",
          "fixes": "the security gate scans for conventional vulnerabilities but not agent-native threats (prompt injection from fetched/MCP content, secret exfiltration in diffs), the attack surface that exists only because an autonomous agent wrote the code."
        },
        {
          "name": "roadmap-crud-add-remove"
        },
        {
          "name": "roadmap-edit-move"
        },
        {
          "name": "roadmap-phase-ops"
        }
      ]
    },
    {
      "name": "Phase 12: Self-Improvement",
      "note": "Centinela applies its own founding principle to itself: a recurring friction\nsignal is an engineering problem to fix permanently — by generating automation.\nComes last: it depends on mature instrumentation from Phase 7.",
      "features": [
        {
          "name": "adaptive-skill-synthesis",
          "dependsOn": [
            "governance-telemetry",
            "centinela-insights"
          ],
          "description": "When telemetry shows a workflow isn't running as expected — e.g. the same feature/step requires too many manual validation rounds, repeated rework, or the user keeps hand-correcting the same thing — Centinela proposes and scaffolds a **skill** (a Claude Code / OpenCode skill, or a reusable validate command / sub-agent prompt) that automates the repetitive step further. Generated skills are reviewable artifacts the user approves before they take effect — never silently self-modifying.",
          "fixes": "the human becomes the slow loop, re-validating the same step over and over, with no path for the harness to absorb that toil."
        },
        {
          "name": "cost-observability-ledger",
          "dependsOn": [
            "cost-governance",
            "governance-telemetry"
          ],
          "description": "A historical, queryable ledger layered on cost-governance: records per-feature/per-step token and model-tier spend over time, supports showback/chargeback rollups, and flags anomalies (e.g. a step whose cost regressed 4x). Complements the preventive budget gate with explanatory analytics, mirroring the proven telemetry/insights append-and-rollup pattern. Acceptance: each governed step appends a spend record; `centinela cost report` rolls up by feature/step/model; an injected cost spike triggers an anomaly flag; the ledger survives across features and is git-tracked durable state.",
          "fixes": "per-step budgets prevent runaway spend but leave no history, so teams cannot attribute, trend, or anomaly-detect token cost across features and steps."
        },
        {
          "name": "governance-roi-eval-harness",
          "dependsOn": [
            "governance-telemetry",
            "centinela-insights"
          ],
          "description": "Deterministic A/B evaluation over a committed fixture: a recorded multi-step agent transcript whose steps introduce planted, oracle-known bugs. The intervention model is veto-and-drop (no live agent, no corrective branching): the governed arm replays each recorded step and runs gates + claim-verification against that step's diff; a step whose diff fails a gate or makes a false claim is REJECTED and its diff is not applied to the accumulating tree, so the governed final diff omits the vetoed buggy steps. The ungoverned arm applies every recorded step unconditionally. Both arms are fully deterministic because the transcript is fixed and the gates are deterministic. Metrics: surviving-bug count (planted bugs present in each arm's final diff per the oracle test suite), plus vetoed-step count, claim-verification rejections, and token cost; output is a reviewable delta report. Acceptance: `centinela eval` yields identical results on repeated runs of the fixture; the governed arm's surviving-bug count is strictly lower than the ungoverned arm's because vetoed steps don't land; a deliberately weakened profile (fewer active gates) vetoes fewer steps and measurably raises the governed arm's surviving-bug count.",
          "fixes": "Centinela asserts it improves correctness but can't measure it, so there's no governed-vs-ungoverned baseline to prove ROI or catch governance regressions over time."
        }
      ]
    },
    {
      "name": "Phase 13: Lighter Centinela",
      "note": "Retrospective-driven refactor (retrospective.md): verify the work, not artifacts about the work; process weight proportional to risk",
      "features": [
        {
          "name": "merge-truthful-delivery",
          "archetype": "canonical",
          "description": "Fix deliver --via merge false success: resolve the primary worktree, verify the target ref advanced and the worktree was removed before reporting success"
        },
        {
          "name": "truthful-validators",
          "archetype": "canonical",
          "description": "Fix nil-uiPaths evidence validate, detect skipped acceptance scenarios (skips fail validate by default), honest quality-score shape errors, honest gate reporting for file-size cap and single-locale i18n"
        },
        {
          "name": "guided-by-default",
          "dependsOn": [
            "truthful-validators"
          ],
          "archetype": "canonical",
          "description": "Flip the default enforcement profile strict to guided, slim the greenfield cascade to PROJECT.md + roadmap.json, delete the self-graded quality threshold gate"
        },
        {
          "name": "coupled-steps-relaxation",
          "archetype": "refactor",
          "description": "workflow.coupled_tests knob (auto-on for compiled languages): allow existing-test edits during code step and additive spec edits during tests step so the tree compiles at every step boundary; surface workflow archetypes at start"
        },
        {
          "name": "token-diet",
          "archetype": "refactor",
          "description": "Drop the O(N) plan-input glob, fix the UX evidence tag matcher, digest-dedup the per-prompt roadmap summary, replace dated model pins with family aliases"
        },
        {
          "name": "roadmap-state-hygiene",
          "archetype": "canonical",
          "description": "Every roadmap CLI mutation regenerates ROADMAP.md and auto-commits roadmap state (pathspec, respects disable_auto_commit); roadmap backlog --stale aging and a completion nudge for deferred findings"
        },
        {
          "name": "smoke-verification",
          "archetype": "canonical",
          "description": "First-class validate.smoke_commands that exercise the running product during validate and merge; smoke verify claim, scaffolded config examples, and a repo smoke script"
        },
        {
          "name": "unified-plan-specialist",
          "description": "Merge the plan-step big-thinker and feature-specialist roles into one reasoning-tier planner: a single subagent context whose prompt carries both lenses as ordered sections (strategy/scope/risks, then acceptance criteria/edge cases/spec), producing one evidence pair instead of two; keeps the after-plan human confirmation and accepts legacy two-role evidence for in-flight workflows."
        },
        {
          "name": "adversarial-validate-verifier",
          "description": "Recast the validate-step gatekeeper as a fresh-context adversarial verifier: refutation stance (find why the completion claim is false; default to not-done when uncertain), evidence-only inputs (diff vs base, frozen spec, gate outputs — never role reports or orchestrator narrative), and it re-runs centinela validate and the test suite itself instead of trusting reports; drops the validation-specialist role; CRITICAL verdict blocks complete."
        },
        {
          "name": "spec-conflict-false-positives",
          "archetype": "hotfix",
          "description": "Fix DetectSpecConflicts false positives that block deliver --via merge: byte-identical spec copies across trees conflict with themselves, and any two scenarios sharing a Given with different outcomes are flagged even within one file (normal companion Gherkin); conflict only on same (file, scenario) diverging across different owners, and dedupe the report (was 720KB of repeats)"
        },
        {
          "name": "docs-step-markdown-first",
          "archetype": "canonical",
          "description": "Redesign the docs step around markdown for humans: delete the HTML pipeline (internal/docgen, docs generate, KB html, portal index), gate on real updated doc files (docs/guides/, README) instead, and add a curated 'docs context' command so the specialist reads feature-scale inputs instead of crawling the repo"
        },
        {
          "name": "docstring-gate",
          "dependsOn": [
            "docs-step-markdown-first"
          ],
          "archetype": "canonical",
          "description": "Deterministic docstring gate: 'centinela docs lint' walks the AST and fails on exported identifiers without doc comments, changed-files ratchet scope; senior-engineer prompt gains the write-doc-comments-in-code-step duty so a Docusaurus/godoc pipeline can consume the codebase later"
        },
        {
          "name": "dynamic-model-routing",
          "archetype": "canonical",
          "description": "Opt-in dynamic model routing: [orchestration] routing_mode = static (default, unchanged) | dynamic; in dynamic mode the orchestrator decides each role's model tier per feature via 'centinela route set <feature> <role> <tier>' persisted in workflow state, with configurable per-role floors (default gatekeeper=reasoning, planner=balanced), downgrade-only-before-step-start / upgrade-anytime, --reason required on downgrades below the static default, and every route decision recorded in telemetry; un-routed roles fall back to static config"
        },
        {
          "name": "dynamic-routing-clock-skew",
          "archetype": "hotfix",
          "description": "Hotfix: dynamic model routing clock-skew handling"
        },
        {
          "name": "binding-evidence-gates",
          "archetype": "canonical",
          "description": "Make three evidence/artifact gates actually bind what they claim: validate handoffTo against the documented role chain (today handoffTo=banana completes a step), validate the centinela:verification commands schema at artifact stamp time instead of accepting any shape, and reject an unfilled <FILL:> template as a changelog (today the stub satisfies the docs gate)"
        },
        {
          "name": "beg-docstring-debt",
          "archetype": "hotfix",
          "description": "Hotfix: document the two exported identifiers in internal/orchestration/evidence.go (Evidence, ValidateEvidence) that the newly-enforcing docstring gate surfaces on binding-evidence-gates' changed-file set"
        },
        {
          "name": "evidence-schema-skeleton-legacy-handoff"
        }
      ]
    },
    {
      "name": "Backlog",
      "features": [
        {"name":"rawio-reformat-diff-churn","summary":"First defer/promote reformats untouched phases of roadmap.json, creating spurious git diff churn","source":{"feature":"deferred-findings-roadmap-capture","role":"senior-engineer"},"deferredAt":"2026-06-12T16:29:13Z"},
        {"name":"roadmap-import-graph-layer-mapping","summary":"Map internal/roadmap as an import_graph layer so the gates->roadmap and ui->roadmap read-only edges are mechanically enforced, not just documented in PROJECT.md G2","source":{"feature":"roadmap-doc-sync","role":"gatekeeper"},"deferredAt":"2026-06-14T10:30:31Z"},
        {"name":"non-go-source-import-graphs","summary":"Parse source-level import graphs for non-Go languages (JS/TS, Ruby, Rust, Python); analyze v1 records declared manifest deps only","source":{"feature":"deep-codebase-analysis","role":"big-thinker"},"deferredAt":"2026-06-17T21:17:33Z"},
        {"name":"brownfield-framework-fingerprinting","summary":"Detect frameworks (Rails/Next/Django/etc.) via directory+dependency heuristics beyond manifest scripts in centinela analyze","source":{"feature":"deep-codebase-analysis","role":"big-thinker"},"deferredAt":"2026-06-17T21:17:33Z"},
        {"name":"incremental-codebase-analysis","summary":"Incremental/cached re-analysis that only re-scans changed directories in centinela analyze","source":{"feature":"deep-codebase-analysis","role":"big-thinker"},"deferredAt":"2026-06-17T21:17:33Z"},
        {"name":"codebase-metrics-enrichment","summary":"Enrich analyze inventory with LOC, complexity, churn, and test-coverage inference","source":{"feature":"deep-codebase-analysis","role":"big-thinker"},"deferredAt":"2026-06-17T21:17:33Z"},
        {"name":"brownfield-route-flow-extraction","summary":"Framework-specific HTTP route and call-flow extraction for reconstructed specs; spec-reconstruction v1 ships package/manifest-derived targets only","source":{"feature":"spec-reconstruction","role":"big-thinker"},"deferredAt":"2026-06-22T07:52:17Z"},
        {"name":"centinela-changelog-subcommand","summary":"Standalone centinela changelog subcommand for merge-path changelog parity (independent of PR delivery)","source":{"feature":"delivery-artifact-generation","role":"big-thinker"},"deferredAt":"2026-06-25T16:36:23Z"},
        {"name":"gitignore-durable-state-guard","summary":"Add a test asserting durable .workflow state (roadmap{,-analysis,-quality}.json, audit-baseline.json) stays tracked/not-gitignored, so the evidence-footprint allow-list can't silently drop bootstrap state again (regression fixed in 420ab3c)","source":{"feature":"lean-evidence-footprint","role":"validation-specialist"},"deferredAt":"2026-06-29T15:08:01Z"},
        {"name":"selfupdate-notice-http-timeout","summary":"centinela update + startup notice use http.DefaultClient (no timeout); a stalled GitHub connection can block session start indefinitely — give the production Updater a bounded client timeout (~5s) so the cold-cache notice fails fast and silent","source":{"feature":"cli-self-update","role":"gatekeeper"},"deferredAt":"2026-06-30T09:45:19Z"},
        {"name":"brownfield-onboarding-docs","summary":"Document the brownfield onboarding path (analyze, synthesize, enrich, confirm) in new-project-guide.md","source":{"feature":"brownfield-setup-detection","role":"big-thinker"},"deferredAt":"2026-06-29T17:42:52Z"},
        {"name":"brownfield-manifest-breadth","summary":"Extend HasSource manifest list to pom.xml, composer.json, build.gradle for broader ecosystem coverage","source":{"feature":"brownfield-setup-detection","role":"feature-specialist"},"deferredAt":"2026-06-29T17:46:35Z"},
        {"name":"unit-test-mcp-server-in-memory-transport","summary":"Cover runMcpServe/mcpConnectSelf via an in-memory MCP transport","source":{"feature":"coverage-hardening","role":"big-thinker"},"deferredAt":"2026-06-30T08:12:27Z"},
        {"name":"fault-inject-atomic-write-error-paths","summary":"Cover WriteBytesAtomic and low-level I/O error branches via fault injection","source":{"feature":"coverage-hardening","role":"big-thinker"},"deferredAt":"2026-06-30T08:12:27Z"},
        {"name":"unit-test-vuln-tool-external-seam","summary":"Cover runVulnTool by stubbing the external vulnerability-scanner binary behind a test seam","source":{"feature":"coverage-hardening","role":"big-thinker"},"deferredAt":"2026-06-30T08:12:27Z"},
        {"name":"aider-local-provider-wiring","summary":"Point the Aider/Claude harness at a local endpoint; local block currently wires only OpenCode's provider surface","source":{"feature":"local-harness-support","role":"big-thinker"},"deferredAt":"2026-06-30T14:45:14Z"},
        {"name":"workflow-save-atomic-write","summary":"workflow.Save uses plain os.WriteFile; a crash mid-write can truncate .workflow/<feature>.json — make it write-temp-then-rename","source":{"feature":"workflow-revise-loop","role":"validation-specialist"},"deferredAt":"2026-06-30T17:52:51Z"},
        {"name":"verify-crosscheck-verifier-commands","summary":"Have centinela verify cross-check the verifier's recorded argv/exit codes against its own claim-verification runs; v1 enforces the commands-run record's shape, not its truthfulness","source":{"feature":"adversarial-validate-verifier","role":"big-thinker"},"deferredAt":"2026-07-28T16:23:12Z"},
        {"name":"typed-gate-error-codes","summary":"hook_statusline_rules classifies validate failures by substring-matching error text, misreporting any non-BLOCKING error as MISSING_PROD_READINESS; replace with typed gate error codes","source":{"feature":"adversarial-validate-verifier","role":"big-thinker"},"deferredAt":"2026-07-28T16:23:12Z"},
        {"name":"reuse-prior-test-run-in-complete-verify","summary":"complete_verify.go never sets verify.Deps.PriorTestRun, so complete re-runs the suite executeValidation just ran; wiring the existing seam is the cheapest cut to validate-step wall clock","source":{"feature":"adversarial-validate-verifier","role":"big-thinker"},"deferredAt":"2026-07-28T16:23:20Z"},
        {"name":"mirror-workflow-enforcement-doc","summary":"workflow-enforcement.md sits on mirrorParityAllowlist as 'not mirrored', so scaffolded projects get no copy and edits to it silently never reach them","source":{"feature":"adversarial-validate-verifier","role":"big-thinker"},"deferredAt":"2026-07-28T16:23:20Z"},
        {"name":"verifier-template-status-fail-closed","summary":"gatekeeper-prompt.md Output Format ships '**Status:** SAFE | WARNING | CRITICAL' plus a fully-formed example commands array, so an unedited verbatim copy plus the mandated artifact stamp passes every gate; make the template Status fail-closed (CRITICAL) and the legend unparseable","source":{"feature":"adversarial-validate-verifier","role":"gatekeeper"},"deferredAt":"2026-07-28T22:21:04Z"},
        {"name":"assess-report-before-full-validate-run","summary":"runValidateGates runs executeValidation + claim verification before wf.Complete reaches gatereport.Assess, so an ungrounded stub costs a full validate run (~9 min here) before being refused; hoist the pure-file-read admissibility check ahead of the suite","source":{"feature":"adversarial-validate-verifier","role":"gatekeeper"},"deferredAt":"2026-07-28T22:21:04Z"},
        {"name":"stale-verification-statusline-unreachable","summary":"validateStatusBlock only calls ValidateArtifacts, which never runs the git-backed freshness check, so STALE_VERIFICATION is unreachable and a stale verification shows as 'none / run-validate' while complete hard-blocks","source":{"feature":"adversarial-validate-verifier","role":"gatekeeper"},"deferredAt":"2026-07-28T22:21:04Z"},
        {"name":"merge-assert-branch-ancestor","summary":"RefAdvanced should also assert the feature branch is an ancestor of the advanced target","source":{"feature":"merge-truthful-delivery","role":"gatekeeper"},"deferredAt":"2026-07-29T07:17:21Z"},
        {"name":"render-warn-gate-details","summary":"Surface WARN gate details in deliver output","source":{"feature":"merge-truthful-delivery","role":"gatekeeper"},"deferredAt":"2026-07-29T07:17:28Z"},
        {"name":"merge-dirty-error-names-cause","summary":"Dirty-primary refusal should name the offending paths (e.g. unignored .worktrees/) not just say commit-or-stash","source":{"feature":"merge-truthful-delivery","role":"gatekeeper"},"deferredAt":"2026-07-29T07:17:28Z"},
        {"name":"merge-distinguish-missing-branch","summary":"Nonexistent branch is reported as git-text-conflict; report branch-not-found","source":{"feature":"merge-truthful-delivery","role":"gatekeeper"},"deferredAt":"2026-07-29T07:17:28Z"},
        {"name":"merge-prunable-registry-false-removal","summary":"merge claims 'removed its worktree' while git worktree list still lists the branch's worktree as prunable and the directory survives elsewhere","source":{"feature":"merge-truthful-delivery","role":"gatekeeper"},"deferredAt":"2026-07-29T07:55:04Z"},
        {"name":"merge-pending-marker-dirties-primary-tree","summary":"the pending marker and steward .md are untracked in repos that track .workflow/, so isDirty(primary) refuses merge --continue from both CWDs","source":{"feature":"merge-truthful-delivery","role":"gatekeeper"},"deferredAt":"2026-07-29T07:55:04Z"},
        {"name":"merge-validate-fail-leaves-main-advanced","summary":"a post-merge validate failure leaves the merge commit on main unannounced and merge --continue closes it without re-validating","source":{"feature":"merge-truthful-delivery","role":"gatekeeper"},"deferredAt":"2026-07-29T07:55:04Z"},
        {"name":"merge-force-remove-locked-worktree","summary":"the --force-remove retry advice does not work for a locked worktree (git needs -f -f or unlock)","source":{"feature":"merge-truthful-delivery","role":"gatekeeper"},"deferredAt":"2026-07-29T07:55:04Z"},
        {"name":"codex-claude-role-agent-registry","summary":"Claude/Codex have no per-role agent registry; AC6 codex-equivalent emitter does not exist","source":{"feature":"unified-plan-specialist","role":"gatekeeper"},"deferredAt":"2026-07-29T16:50:42Z"},
        {"name":"managed-agent-retirement-sweep","summary":"Managed-sync sweep to retire replaced agents from existing projects' opencode.json","source":{"feature":"unified-plan-specialist","role":"gatekeeper"},"deferredAt":"2026-07-29T16:50:42Z"},
        {"name":"prompt-doc-budget-ratchet","summary":"Aggregate prompt-doc budget ratchet as fallback when a merged doc exceeds the 130-line cap","source":{"feature":"unified-plan-specialist","role":"gatekeeper"},"deferredAt":"2026-07-29T16:50:42Z"},
        {"name":"statusline-plan-role-display","summary":"Statusline/status has no plan-step role display field","source":{"feature":"unified-plan-specialist","role":"gatekeeper"},"deferredAt":"2026-07-29T16:50:43Z"},
        {"name":"revise-to-plan-sheds-no-evidence","summary":"revise --to plan invalidates no plan evidence; next complete re-passes on stale artifacts","source":{"feature":"unified-plan-specialist","role":"gatekeeper"},"deferredAt":"2026-07-29T16:50:43Z"},
        {"name":"legacy-plan-key-mixed-form-precedence","summary":"Mixed legacy tier + override table inverts documented precedence and drops the configured tier","source":{"feature":"unified-plan-specialist","role":"gatekeeper"},"deferredAt":"2026-07-29T16:50:43Z"},
        {"name":"planner-skeleton-prompt-header-drift","summary":"Planner companion-stub headers drift from planner-prompt.md sections","source":{"feature":"unified-plan-specialist","role":"gatekeeper"},"deferredAt":"2026-07-29T16:50:43Z"},
        {"name":"stale-spec-text-after-role-retirement","summary":"Foreign specs still assert pre-retirement prompt-doc set (add-plan-advisor-mode, configurable-subagent-models, add-agent-evidence-contract, evidence-cli)","source":{"feature":"unified-plan-specialist","role":"gatekeeper"},"deferredAt":"2026-07-29T16:50:43Z"},
        {"name":"brownmap-roadmap-source-role-retired","summary":"brownmap baseline/gaps still stamp Source.Role=big-thinker on new entries","source":{"feature":"unified-plan-specialist","role":"gatekeeper"},"deferredAt":"2026-07-29T16:50:43Z"},
        {"name":"planner-stub-on-legacy-workflow-unguarded","summary":"evidence init planner on an unpinned workflow is unguarded","source":{"feature":"unified-plan-specialist","role":"gatekeeper"},"deferredAt":"2026-07-29T16:50:43Z"},
        {"name":"cross-process-suite-result-reuse","summary":"Reuse the gatekeeper's suite result in complete via a tamper-evident digest-keyed run record (any plain on-disk record is writable by the agent it would excuse)","source":{"feature":"dedupe-validate-suite-runs","role":"planner"},"deferredAt":"2026-07-29T20:26:54Z"},
        {"name":"validate-flake-diagnosability","summary":"A validate suite command can fail without naming any failing test in the rendered output (observed once: go test ./... -coverprofile exit!=0 with only ok lines shown); persist full suite output to a log for diagnosis","source":{"feature":"dedupe-validate-suite-runs","role":"gatekeeper"},"deferredAt":"2026-07-29T21:48:21Z"},
        {"name":"migrate-prune-project-docs","summary":"centinela migrate leaves stale generated docs/project-docs/ output in downstream projects after the HTML pipeline removal; add a migrate cleanup that prunes it","source":{"feature":"docs-step-markdown-first","role":"planner"},"deferredAt":"2026-07-30T08:39:23Z"},
        {"name":"migrate-doc-version-refresh","summary":"Rewritten managed docs (documentation-generator-prompt.md et al) never reach downstream repos: migrate drift detection is doc-version-only (CurrentDocVersion stays 1), so content rewrites under the same version are invisible to BuildPlan; needs a content-hash or version-bump strategy","source":{"feature":"docs-step-markdown-first","role":"senior-engineer"},"deferredAt":"2026-07-30T11:42:16Z"},
        {"name":"stale-project-docs-pruning","summary":"Downstream repos keep a stale generated docs/project-docs/ portal after portal regen removal; add migrate-time pruning","source":{"feature":"docs-step-markdown-first","role":"edge-case-tester"},"deferredAt":"2026-07-30T11:50:54Z"},
        {"name":"changelog-scanner-line-limit","summary":"validateChangelog ignores bufio.Scanner errors, so a >64KB single-line changelog is misreported as empty","source":{"feature":"docs-step-markdown-first","role":"edge-case-tester"},"deferredAt":"2026-07-30T11:50:54Z"},
        {"name":"legacy-docs-spec-scenarios-cleanup","summary":"Legacy spec scenario rewrites (right-size-docs-step, docs-knowledge-base-pages, generate-html-project-docs, improve-docs-llm-hybrid-ui, docs-latest-features-getting-started, merge-truthful-delivery portal scenario) still describe the deleted HTML/KB pipeline; prewrite hook blocks specs/ writes during the tests step, so the contradicted scenarios must be rewritten in a plan-step-capable follow-up.","source":{"feature":"docs-step-markdown-first","role":"qa-senior"},"deferredAt":"2026-07-30T12:15:01Z"},
        {"name":"spec-conflict-scenario-deletion-detection","summary":"Spec-conflict detector ignores scenario deletion: a worktree that removes a scenario another worktree edited is invisible to DetectSpecConflicts","source":{"feature":"spec-conflict-false-positives","role":"senior-engineer"},"deferredAt":"2026-07-30T07:54:20Z"},
        {"name":"spec-conflict-deep-gherkin-diff","summary":"Spec-conflict detector compares only the first Given and first Then per scenario; divergence in When/And/second-Then steps is not detected","source":{"feature":"spec-conflict-false-positives","role":"senior-engineer"},"deferredAt":"2026-07-30T07:54:24Z"},
        {"name":"spec-conflict-precheck-requires-merging-worktree","summary":"centinela merge's spec-conflict pre-check is silently disarmed whenever .worktrees/<merging-feature>/specs is absent (worktree already removed by a prior partial merge, --force-remove, or raw-PR delivery): DetectSpecConflicts returns nil before reading any other worktree, so a genuine two-worktree divergence merges unblocked","source":{"feature":"spec-conflict-false-positives","role":"gatekeeper"},"deferredAt":"2026-07-30T08:53:19Z"},
        {"name":"spec-conflict-cross-file-contradiction-unbacked","summary":"Cross-file spec contradictions (two spec files asserting opposite outcomes for the same Given) are no longer detected and no gate backstops them: centinela validate runs file_size/build/import_graph/spec_traceability plus go test+coverage+fmt, none of which compares Gherkin semantics across files; the trade-off is sound but the documented backstop does not exist","source":{"feature":"spec-conflict-false-positives","role":"gatekeeper"},"deferredAt":"2026-07-30T08:53:19Z"},
        {"name":"model-capability-spec-scenario-uncovered","summary":"model-capability-profiles.feature scenario lacks acceptance coverage","source":{"feature":"token-diet","role":"gatekeeper"},"deferredAt":"2026-07-30T09:14:14Z"},
        {"name":"legacy-model-specs-have-no-scenario-markers","summary":"28 pre-existing legacy model spec scenarios have no acceptance markers","source":{"feature":"token-diet","role":"gatekeeper"},"deferredAt":"2026-07-30T09:14:14Z"},
        {"name":"plan-empty-inputs-message-masked","summary":"E4 empty-inputs message unreachable behind snapshot check","source":{"feature":"token-diet","role":"gatekeeper"},"deferredAt":"2026-07-30T09:14:14Z"},
        {"name":"summary-digest-hashes-nonschedulable-phases","summary":"roadmap summary digest over-renders on Backlog-only changes","source":{"feature":"token-diet","role":"gatekeeper"},"deferredAt":"2026-07-30T09:14:14Z"},
        {"name":"hook-context-panel-diet","summary":"ACTIVE WORKFLOWS + nudge panels are 96% of hook context bytes; diet them next","source":{"feature":"token-diet","role":"gatekeeper"},"deferredAt":"2026-07-30T09:14:15Z"},
        {"name":"scaffold-toml-orchestration-examples","summary":"Scaffolded centinela.toml documents workflow/gates knobs but carries zero [orchestration] examples for already-shipped features (models, model_map, capabilities, local backend) — new projects discover them only via docs/guides; add a commented orchestration example block","source":{"feature":"dynamic-model-routing","role":"planner"},"deferredAt":"2026-07-30T17:09:41Z"},
        {"name":"workflow-state-atomic-save","summary":"workflow.Save is an unlocked, non-atomic whole-file WriteFile; concurrent route set + complete lose updates and a killed write bricks the feature state","source":{"feature":"dynamic-model-routing","role":"edge-case-tester"},"deferredAt":"2026-07-30T18:12:16Z"},
        {"name":"workflow-state-schema-version","summary":"No schema version on .workflow/*.json: an older binary silently drops modelRoutes (and any newer field) on every load-save round-trip","source":{"feature":"dynamic-model-routing","role":"edge-case-tester"},"deferredAt":"2026-07-30T18:12:16Z"},
        {"name":"route-lifecycle-hygiene","summary":"Recorded routes are invisible and unmanageable outside the dynamic window: flipping routing_mode to static hides them from route show/set while leaving them re-armable (E11), route show succeeds on a done workflow while route set refuses (E14), and a past-step upgrade is accepted and emits telemetry nothing consumes (E13). Needs a route clear/list surface and a scheduling-aware rule 4.","source":{"feature":"dynamic-model-routing","role":"qa-senior"},"deferredAt":"2026-07-30T18:39:42Z"},
        {"name":"unaudited-handwritten-routes-for-floorless-roles","summary":"A route hand-written into .workflow/<feature>.json takes effect for any role with no configured floor, with no route-decision telemetry event — floors and the audit trail both bind only what route set writes, so 'every route decision is audited' holds for the CLI path only","source":{"feature":"dynamic-model-routing","role":"gatekeeper"},"deferredAt":"2026-07-31T08:14:51Z"},
        {"name":"start-resume-skips-routing-directive","summary":"runStart returns early on the worktree 'Resuming existing workflow' branch before printRoutingDirective, so a resuming start prints no routing line (the orchestration hook re-emits it, so the decision window is not lost)","source":{"feature":"dynamic-model-routing","role":"gatekeeper"},"deferredAt":"2026-07-31T08:14:51Z"},
        {"name":"acceptance-verbose-terminator-attribution-steal","summary":"Verbose go test terminator attribution steal — unindented nested terminators can misattribute an acceptance skip","source":{"feature":"truthful-validators","role":"gatekeeper"},"deferredAt":"2026-07-31T08:42:41Z"},
        {"name":"acceptance-skip-parser-breadth","summary":"Skip parser breadth beyond cucumber/godog/go-test (pytest-bdd, behave, JVM cucumber, cucumber-rs)","source":{"feature":"truthful-validators","role":"gatekeeper"},"deferredAt":"2026-07-31T08:42:41Z"},
        {"name":"self-validate-acceptance-report-shape","summary":"Centinela's own non-verbose coverage command carries no skip data — consider a repo-level acceptance command that does","source":{"feature":"truthful-validators","role":"gatekeeper"},"deferredAt":"2026-07-31T08:42:41Z"},
        {"name":"validate-command-verdicts-in-machine-output","summary":"Per-command acceptance verdicts are terminal-only; centinela verdict/pr-gate carry no validate-command line","source":{"feature":"truthful-validators","role":"gatekeeper"},"deferredAt":"2026-07-31T08:42:41Z"},
        {"name":"acceptance-tier-attribution-path-hardcoded","summary":"internal/acceptance hardcodes tests/acceptance (case-sensitive); projects with another acceptance tier path get detection silently disarmed under whole-repo commands — needs a config knob","source":{"feature":"truthful-validators","role":"gatekeeper"},"deferredAt":"2026-07-31T08:42:41Z"},
        {"name":"roadmap-checkpoint-marker-truncation","summary":"internal/roadmapcheckpoint compares sub-second artifact mtimes against an RFC3339 second-truncated marker, so centinela roadmap iterate's suppression re-fires immediately — same defect class as the dynamic-routing clock-skew hotfix, reproduced by probe","source":{"feature":"dynamic-routing-clock-skew","role":"gatekeeper"},"deferredAt":"2026-07-31T09:37:25Z"},
        {"name":"broaden-fill-marker-stub-detection","summary":"internal/evidence FillMarker/<FILL:> stub-detection is changelog-only for binding-evidence-gates v1; broaden to all companion .md reports IF an unfilled-companion-report-passes-gate bug is hit in practice like dynamic-model-routing's changelog case (risk: false positives on legitimate prose mentioning the marker)","source":{"feature":"binding-evidence-gates","role":"planner"},"deferredAt":"2026-07-31T10:05:13Z"},
        {"name":"retire-handoff-alternate-pin-tolerance","summary":"handoffTo chain validation accepts the successor STEP's occupant under either contract pin (validate: gatekeeper|validation-specialist, plan: planner|big-thinker) so evidence written by the pre-fix hardcoded prefill still validates. Once no in-flight or downstream workflow carries a pre-gate handoffTo, tighten acceptsHandoff to exact-match on the derived successor and delete alternateContractRoles.","source":{"feature":"binding-evidence-gates","role":"senior-engineer"},"deferredAt":"2026-07-31T11:03:58Z"},
        {"name":"workflow-state-file-disarms-its-own-gates","summary":"orchestrationMode:\"\" or a deleted .workflow/<feature>.json silently disables the whole strict-orchestration gate, and validateContract:\"\" disables the grounded-verdict gate — the agent-writable state file is the trust root for both","source":{"feature":"binding-evidence-gates","role":"edge-case-tester"},"deferredAt":"2026-07-31T11:19:32Z"},
        {"name":"evidence-validate-skips-handoff-chain","summary":"centinela evidence validate and evidence set both accept an out-of-chain handoffTo that centinela complete rejects, so the agents' own pre-flight command disagrees with the gate","source":{"feature":"binding-evidence-gates","role":"edge-case-tester"},"deferredAt":"2026-07-31T11:19:32Z"},
        {"name":"changelog-substance-floor-is-non-empty","summary":"once the FILL marker is gone validateChangelog accepts a bare '-' or 'x' as an entry; no conventional-commit shape or minimum substance is required","source":{"feature":"binding-evidence-gates","role":"edge-case-tester"},"deferredAt":"2026-07-31T11:19:32Z"},
        {"name":"fill-marker-detection-is-exact-substring","summary":"HasFillMarker matches only the literal '<FILL:' so '<fill:', '< FILL:' and '<FILL type>' pass, while a legitimate changelog quoting the marker in prose is a false positive","source":{"feature":"binding-evidence-gates","role":"edge-case-tester"},"deferredAt":"2026-07-31T11:19:32Z"},
        {"name":"pin-feature-surface-on-workflow","summary":"IsUserFacingFeature greps docs/features/<feature>.md at gate time, so deleting the surface line mid-workflow silently drops the ux-ui + docs-specialist requirements and changes the derived handoff successor; ValidateContract/PlanContract are pinned in workflow state at start for exactly this reason, surface is not","source":{"feature":"binding-evidence-gates","role":"gatekeeper"},"deferredAt":"2026-07-31T12:41:23Z"},
        {"name":"changelog-bare-heading-passes-gate","summary":"validateChangelog counts every non-blank line as an entry, so a file containing only '## Changelog' passes; needs a substance floor distinguishing a heading from an entry","source":{"feature":"binding-evidence-gates","role":"gatekeeper"},"deferredAt":"2026-07-31T12:41:23Z"},
        {"name":"fill-marker-codespan-false-positive","summary":"A concrete <FILL: type> quoted inside a backtick code span is refused as a stub; detector is also case-sensitive — quoting the concrete marker form should be legal in prose","source":{"feature":"binding-evidence-gates","role":"gatekeeper"},"deferredAt":"2026-07-31T12:41:23Z"},
        {"name":"changelog-erasure-passes-gate","summary":"StubEntry only fires while a literal <FILL: survives, so deleting the scaffold's slots in place leaves '- : ' (or '-', '.', 'TODO') and validateChangelog accepts it; the placeholder rule needs a substance floor beside it — same gap as changelog-bare-heading-passes-gate","source":{"feature":"binding-evidence-gates","role":"gatekeeper"},"deferredAt":"2026-07-31T13:14:22Z"},
        {"name":"feature-surface-unpinned-at-gate-time","summary":"IsUserFacingFeature re-reads docs/features/<feature>.md at gate time: deleting the 'surface: user-facing' line mid-workflow flips a refused senior-engineer handoff to a pass and drops the ux-ui-specialist requirement; validateContract/planContract are pinned in workflow state at start, surface is not","source":{"feature":"binding-evidence-gates","role":"gatekeeper"},"deferredAt":"2026-07-31T13:14:22Z"},
        {"name":"docstring-generated-banner-visibility","summary":"Generated-file banner is a silent whole-file bypass; not counted, listed, or marked exempt","source":{"feature":"docstring-gate","role":"gatekeeper"},"deferredAt":"2026-08-03T08:12:12Z"},
        {"name":"docstring-full-scan-empty-roots-honesty","summary":"docs lint --full reports a confident 0-of-0 when roots match nothing","source":{"feature":"docstring-gate","role":"gatekeeper"},"deferredAt":"2026-08-03T08:12:12Z"},
        {"name":"docstring-ratchet-content-change-only","summary":"chmod/rename pulls a legacy file's whole backlog into scope","source":{"feature":"docstring-gate","role":"gatekeeper"},"deferredAt":"2026-08-03T08:12:12Z"},
        {"name":"docstring-gate-scenario-clause-coverage","summary":"Two scenarios asserted via docs lint substitutes rather than the gate surface","source":{"feature":"docstring-gate","role":"gatekeeper"},"deferredAt":"2026-08-03T08:12:12Z"},
        {"name":"gate-pass-details-invisible","summary":"RenderGateResult expands Details on Fail only, hiding checkFileSize justified list and checkDocstring exemptions on passing runs","source":{"feature":"docstring-gate","role":"gatekeeper"},"deferredAt":"2026-08-03T08:12:12Z"},
        {"name":"docstring-nodoc-spaced-spelling","summary":"Spaced // centinela:nodoc counts as documented and is never listed as exempt","source":{"feature":"docstring-gate","role":"gatekeeper"},"deferredAt":"2026-08-03T08:12:12Z"},
        {"name":"docstring-full-scan-symlink-divergence","summary":"WalkDir does not follow symlinks so --full misses a tree InScope accepts","source":{"feature":"docstring-gate","role":"gatekeeper"},"deferredAt":"2026-08-03T08:12:12Z"},
        {"name":"gitdiff-remote-fallback-skips-hierarchical-base","summary":"origin/<base> retry is skipped for any base containing a slash (release/2.0)","source":{"feature":"docstring-gate","role":"gatekeeper"},"deferredAt":"2026-08-03T08:12:12Z"},
        {"name":"pr-gate-ci-scope-narrowed-by-remote-fallback","summary":"pr-gate CI scope narrows full-repo to diff-aware now that the base resolves","source":{"feature":"docstring-gate","role":"gatekeeper"},"deferredAt":"2026-08-03T08:12:12Z"},
        {"name":"docstring-ci-scenario-ratchet-clause-unasserted","summary":"CI scenario ratchet clause is not actually asserted by its test fixture","source":{"feature":"docstring-gate","role":"gatekeeper"},"deferredAt":"2026-08-03T08:12:12Z"},
        {"name":"docstring-roots-parent-escape-unvalidated","summary":"A ..-escaping root passes absolute-root validation but walks outside the repo","source":{"feature":"docstring-gate","role":"gatekeeper"},"deferredAt":"2026-08-03T08:12:12Z"},
        {"name":"docstring-pass-message-plural-disagreement","summary":"Pass message says 'All 1 exported identifiers' — plural disagreement","source":{"feature":"docstring-gate","role":"gatekeeper"},"deferredAt":"2026-08-03T08:12:12Z"},
        {"name":"evidence-doc-comment-overclaims-handoff","summary":"The ValidateEvidence doc comment says it enforces the handoff-successor rule; it enforces only the merge-steward literal complete/user pair — the derived-successor check lives in internal/workflow.CheckHandoffTo","source":{"feature":"beg-docstring-debt","role":"gatekeeper"},"deferredAt":"2026-08-03T09:35:29Z"},
        {"name":"acceptance-tier-exceeds-default-test-timeout","summary":"tests/acceptance takes ~442s alone and hit 'panic: test timed out after 10m0s' under full-suite parallelism; validate.commands passes no -timeout, so every validate run carries a live flake risk that presents as an unexplained panic rather than a test failure","source":{"feature":"evidence-schema-skeleton-legacy-handoff","role":"qa-senior"},"deferredAt":"2026-08-03T12:40:20Z"},
        {"name":"prewrite-hook-cwd-relative-workflow-lookup","summary":"the prewrite hook resolves the active workflow through the same CWD-relative lookup fixed in evidence schema (hook_workflows.go), so an agent working from a subdirectory of its own worktree has legitimate writes blocked","source":{"feature":"evidence-schema-skeleton-legacy-handoff","role":"qa-senior"},"deferredAt":"2026-08-03T12:40:20Z"},
        {"name":"evidence-active-workflow-corrupt-json-ambiguity","summary":"ActiveWorkflows warns to stderr and skips an unparseable .workflow/*.json, so a corrupt sibling can hide a second active workflow and let ResolveActiveFeature answer confidently where it should decline","source":{"feature":"evidence-schema-skeleton-legacy-handoff","role":"edge-case-tester"},"deferredAt":"2026-08-03T12:40:30Z"},
        {"name":"workflow-warnings-break-json-stdout-capture","summary":"workflow load warnings go to stderr, so the standard agent capture 'centinela evidence schema <role> 2>&1' yields unparseable output despite the command's JSON-for-prompt-embedding contract","source":{"feature":"evidence-schema-skeleton-legacy-handoff","role":"edge-case-tester"},"deferredAt":"2026-08-03T12:40:30Z"},
        {"name":"handoff-gate-skips-nonrequired-role-evidence","summary":"CheckHandoffTo only runs for roles the workflow's contract requires, so evidence for a non-required role passes validate with any handoffTo — including the unfilled <successor-role> placeholder","source":{"feature":"evidence-schema-skeleton-legacy-handoff","role":"edge-case-tester"},"deferredAt":"2026-08-03T12:40:30Z"},
        {"name":"start-inside-worktree-creates-nested-worktree","summary":"centinela start provisions worktrees with repo=\".\" and has no inside-a-worktree guard, so starting a feature from within a worktree creates .worktrees/A/.worktrees/B; that nested layout makes nested-worktrees-resolve-outermost reachable with real workflow state on both levels, so evidence schema prints the outer feature's slug and a handoffTo the inner feature's gate refuses","source":{"feature":"evidence-schema-skeleton-legacy-handoff","role":"gatekeeper"},"deferredAt":"2026-08-03T13:05:26Z"},
        {"name":"nested-worktrees-resolve-outermost","summary":"a path with repeated .worktrees segments resolves the OUTERMOST feature; verified NOT harmless — the outer feature has real workflow state, so evidence schema prints that feature's successor confidently for work happening in the inner one (probed: outer-uf/ux-ui-specialist where the truth was inner-feat/qa-senior)","source":{"feature":"evidence-schema-skeleton-legacy-handoff","role":"gatekeeper"},"deferredAt":"2026-08-03T13:14:44Z"}
      ]
    }
  ],
  "intro": "Centinela is a harness-governance layer for AI coding agents. Its guiding\nprinciple: **treat every agent failure as an engineering problem to fix\npermanently, not a prompt to retry — make correctness enforced, not requested.**\nEach item below names the agent or team failure mode it permanently fixes.\n\n**Capability-spectrum principle.** Centinela must govern *any* model — from a\nsmall local model running on a laptop to a frontier reasoning model. The\nweaker the model, the more scaffolding it needs (step-gating, artifact\ntemplates, confirmations); the stronger the model, the more enforcement\nshifts to outcomes (gates + claim verification at delivery). What never\nvaries is independent mechanical verification: no model's claims are\ntrusted, ever. **Governance adapts; verification is constant.**\n\nStatus is derived from workflow state — run `centinela roadmap` for the live view.\nPhases are ordered by leverage and dependency; scheduling follows the\n`dependsOn` graph, not the phase number."
}
