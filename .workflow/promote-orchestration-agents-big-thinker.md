# Orchestration Evidence: big-thinker

- Feature: `promote-orchestration-agents`
- Step: `plan`
- Outcome: Framed the asymmetry between four full report-agent prompt files and six terse embedded role strings; scoped the fix as a doc-only additive change with a per-file length budget (≤70 lines), scaffold mirroring, and zero runtime code modifications. Identified `validation-specialist` as the highest-value target since it was newly added and has no companion prompt yet.
- Handoff: `feature-specialist`
