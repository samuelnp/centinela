# Edge-Case Report: landing-page

Generated by qa-senior on 2026-05-25.

## Hard paths guarded by the test suite

| Edge Case | Risk | Executable Assertion |
|-----------|------|----------------------|
| **No-JS render** | Sections hidden behind reveal animation are never visible when JS is disabled | `TestLandingPage_NoJS_RevealGating`: asserts `html.js .reveal` selector is present, meaning the hidden-state rule is scoped to JS-enhanced documents only; without JS the class is never applied and content is always visible at full opacity |
| **prefers-reduced-motion** | Animations fire even for motion-sensitive users | `TestLandingPage_ReducedMotionGuard`: asserts `prefers-reduced-motion` media query is present in the stylesheet; all transitions/animations are wrapped inside `@media (prefers-reduced-motion: no-preference)` so they are suppressed for `reduce` users |
| **≤360px no horizontal overflow — nav-collapse specificity regression** | `.nav-links a{display:inline-flex}` (specificity 0,1,1) silently overrides `.hide-sm{display:none}` (0,1,0), re-showing nav links and causing 528px scrollWidth at a 360px viewport | `TestLandingPage_NavCollapseSpecificityRegression`: asserts the rule `.nav-links a.hide-sm{display:none}` (specificity 0,2,1) is present, ensuring it beats the tap-target rule |
| **demo.gif 404** | Layout collapses or JS error thrown when GIF is unavailable | `TestLandingPage_DemoGifAttributes`: asserts `loading="lazy"`, `decoding="async"`, explicit `width="1200"` and `height="700"` are set; the CSS `aspect-ratio:1200/700` and `background:var(--bg-2)` reserve the box; no image-load JS handlers exist |
| **Absolute OG URLs** | Social-preview card broken on Slack/Discord/X when URL is relative | `TestLandingPage_OGMetaTags`: asserts `og:image` and `twitter:image` both contain the absolute URL `https://samuelnp.github.io/centinela/assets/social-preview.png`; `og:url` is the exact canonical URL |
| **Exact install string** | Copy-paste installs wrong version or path | `TestLandingPage_InstallCommand`: asserts the exact string `go install github.com/samuelnp/centinela@latest` is present |
| **No external CDN** | Page breaks if CDN is down; runtime JS framework loaded from external host | `TestLandingPage_NoExternalDeps`: regex-asserts no `<script src="http...">` and no `<link rel=stylesheet href="http...">` exist; favicon `data:` URI is excluded from this check |
| **Green→blue palette regression** | Reverted neon-green accent (`#3fff9f`) reintroduced after the user-requested palette swap | `TestLandingPage_PaletteBlueNotGreen`: asserts `#40a0e0` (logo azure) is present AND `#3fff9f` (neon green) is absent |
