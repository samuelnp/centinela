- perf: stop padding hook context panels to a fixed width — the spaces squared a box in a terminal but hook output goes into the model's context, where they were invisible and pure token cost. `centinela hook context` drops from 2004 to 1195 bytes per turn with content byte-identical; CLI renderers (`status`, `roadmap`, blocked-write refusals) keep their padding, and a colocated size guard fails if the output grows again
