# Orchestration Evidence: big-thinker

- Feature: `extract-agent-shared-blocks`
- Step: `plan`
- Outcome: Framed the four-cut Tier 2 scope (shared invocation, gatekeeper trim, stack-reference extraction) as the highest-value subset of the prompt-bloat audit recommendations. Deferred Tier 3 doc-version-comment removal because `internal/migration/header.go` parses those comments as managed-doc headers — a manifest-based refactor is required and is out of scope here. Skipped the unified output-format block because existing acceptance tests assert on specific Output-Format substrings, making the refactor higher-risk than the line savings justify.
- Handoff: `feature-specialist`
