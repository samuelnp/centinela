- feat: `centinela doctor` diagnoses project health across seven checks (hook wiring, roadmap drift + phase-name status glyphs, abandoned worktrees, orphaned `.workflow` state, orphaned evidence temp files, config drift, binary version skew); `--fix` applies only safe, idempotent repairs (re-wire hooks, regenerate ROADMAP.md / strip glyphs, sweep `*.json.tmp`) and never performs destructive actions.
