- feat: custom gates — define your own mechanical gates in `[[gates.custom]]` (`name`, `command`, `severity` fail|warn, `output` blob|lines) that run inside `centinela validate` without forking. They produce first-class gate results: warn/fail severity, telemetry, and per-violation baselining by the audit ratchet (`output = "lines"`). Built-in gates are now reference implementations of the shared result contract. Per-gate timeout; trusted checked-in-config exec model (same as `[validate] commands`).
