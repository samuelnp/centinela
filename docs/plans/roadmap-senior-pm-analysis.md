# Plan: Roadmap Senior PM Analysis

1. Add roadmap analysis model + validation in `internal/roadmap`.
2. Enforce readiness in `start` guard for greenfield projects.
3. Add `centinela roadmap validate` command.
4. Update setup hook/UI guidance to request senior PM analysis artifacts.
5. Add unit/integration/acceptance tests for validation and guard behavior.
