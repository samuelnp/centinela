# Plan: Add UX/UI Specialist Orchestration

1. Add feature brief metadata parsing for `surface: user-facing`.
2. Extend configuration with UI path prefixes and defaults.
3. Replace step-only role selection with feature-aware role resolution.
4. Add `ux-ui-specialist` evidence validation with actionable UI output rules.
5. Update orchestration hook output and workflow validation call sites.
6. Add unit, integration, and acceptance tests for user-facing and non-user-facing branches.
7. Update framework docs and scaffold docs to describe the new specialist role.
